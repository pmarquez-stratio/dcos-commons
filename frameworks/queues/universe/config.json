{
  "type": "object",
  "properties": {
    "service": {
      "type": "object",
      "description": "DC/OS service configuration properties",
      "properties": {
        "name": {
          "description": "The name of the service instance",
          "type": "string",
          "default": "queues",
          "title": "Service name"
        },
        "user": {
          "description": "The user that the service will run as.",
          "type": "string",
          "default": "root",
          "title": "User"
        },
        "service_account": {
          "description": "The service account for DC/OS service authentication. This is typically left empty to use the default unless service authentication is needed. The value given here is passed as the principal of Mesos framework.",
          "type": "string",
          "default": ""
        },
        "gpus_enabled": {
          "description": "Enable use of GPU resources",
          "type": "boolean",
          "default": false
        },
        "prereserved_roles": {
          "description": "Comma-separated list of prereserved roles to register against with Mesos, such as 'slave_public'",
          "type": "string",
          "default": ""
        }
      },
      "required": [
        "name",
        "user"
      ]
    },
    "debug": {
      "type": "object",
      "description": "Debug options",
      "properties": {
        "custom_zookeeper": {
          "description": "Configures a custom zookeeper host to use for storing persisted state, of the form 'host:port'",
          "type": "string",
          "default": ""
        },
        "mesos_api_version": {
          "description": "Configures the Mesos API version to use. Possible values: V0 (non-HTTP), V1 (HTTP)",
          "type": "string",
          "enum": [
            "V0",
            "V1"
          ],
          "default": "V1"
        },
        "log_level": {
          "description": "The log level for the DC/OS service.",
          "type": "string",
          "enum": [
            "OFF",
            "FATAL",
            "ERROR",
            "WARN",
            "INFO",
            "DEBUG",
            "TRACE",
            "ALL"
          ],
          "default": "INFO"
        },
        "web_url": {
          "description": "A web URL to configure with Mesos",
          "type": "string",
          "default": ""
        }
      }
    }
  }
}
