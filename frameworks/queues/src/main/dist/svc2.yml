name: barsvc
scheduler:
  user: {{FRAMEWORK_USER}}
pods:
  barpod:
    count: 2
    tasks:
      node:
        goal: RUNNING
        cmd: "echo bar >> output && sleep $SLEEP_DURATION"
        cpus: {{NODE_CPUS}}
        memory: {{NODE_MEM}}
        env:
          SLEEP_DURATION: {{SLEEP_DURATION}}
