name: foosvc
scheduler:
  user: {{FRAMEWORK_USER}}
pods:
  foopod:
    count: 2
    tasks:
      node:
        goal: RUNNING
        cmd: "echo foo >> output && sleep $SLEEP_DURATION"
        cpus: {{NODE_CPUS}}
        memory: {{NODE_MEM}}
        env:
          SLEEP_DURATION: {{SLEEP_DURATION}}
