name: bazsvc
scheduler:
  user: {{FRAMEWORK_USER}}
pods:
  bazpod:
    count: 2
    tasks:
      node:
        goal: RUNNING
        cmd: "echo baz >> output && sleep $SLEEP_DURATION"
        cpus: {{NODE_CPUS}}
        memory: {{NODE_MEM}}
        env:
          SLEEP_DURATION: {{SLEEP_DURATION}}
